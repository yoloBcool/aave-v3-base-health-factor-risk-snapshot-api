# Changelog

## 1.0.1 — 2025-11-10
- CLI: add `--json` for machine-only stdout.
- Tests: `test_local.py` invokes CLI path with `--json`.
- CI: Windows runner validates schema, runs event + CLI, uploads sample artifact.
- Repo: added `.env.example`, Windows helper script, and Makefile.
